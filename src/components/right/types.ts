export type AlternativeThought = {
  thought: string;
  technique: string;
  techniqueDescription: string;
};

export type BibleVerseResult = {
  verse: string;
  reference: string;
  prayer: string;
};
